// oauth.ts — stub vazio. A autenticação OAuth do Manus foi removida.
// A autenticação é feita via e-mail/senha + JWT próprio.
import type { Express } from "express";

export function registerOAuthRoutes(_app: Express) {
  // Nenhuma rota OAuth registrada.
}
